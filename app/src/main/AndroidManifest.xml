<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="chris.accountms">

    <application
        android:allowBackup="true"
        android:icon="@drawable/jizhang"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AccountMS">
        <activity android:name=".Activities.ExpenseChartActivity" />
        <activity android:name=".Activities.NoteActivity" />
        <activity android:name=".Activities.MyExpenseActivity" />
        <activity android:name=".Activities.MyIncomeActivity" />
        <activity android:name=".Activities.SettingActivity" />
        <activity android:name=".Activities.HelpActivity" />
        <activity android:name=".Activities.MyNoteActivity" />
        <activity android:name=".Activities.IncomeChartActivity" />
        <activity android:name=".Activities.ExpenseActivity" />
        <activity android:name=".Activities.IncomeActivity" />
        <activity android:name=".Activities.RegisterActivity" />
        <activity android:name=".Activities.MainActivity">
            <!--            <intent-filter>-->
            <!--                <action android:name="android.intent.action.MAIN" />-->
            <!--                <category android:name="android.intent.category.LAUNCHER" />-->
            <!--            </intent-filter>-->
        </activity>
        <activity android:name=".Activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>